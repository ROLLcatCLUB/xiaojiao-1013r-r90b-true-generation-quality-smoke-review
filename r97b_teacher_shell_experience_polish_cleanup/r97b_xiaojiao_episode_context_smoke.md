# R97B Xiaojiao Episode Context Smoke

本轮验证底部小教上下文可按 5 个 episode 静态切换，不调用 runtime。

- 选择 `episode_01_notice_gradient`：显示 `看见渐变`；提示 `先让学生指出起点和终点，不急着讲术语。`；证据 `记录能用“从___到___”表达变化方向的学生。`。
- 选择 `episode_02_distinguish_brightness_saturation`：显示 `分清亮暗与鲜灰`；提示 `先让学生分亮暗/鲜灰，再补明度/纯度。`；证据 `记录能分类并说明理由的学生。`。
- 选择 `episode_03_three_step_color_trial`：显示 `三格试色`；提示 `先做三格小样，不急着画完整作品。`；证据 `保存三格小样作为过程证据。`。
- 选择 `episode_04_apply_to_artwork`：显示 `放进作品`；提示 `提醒教师分层发布任务，避免所有学生做复杂作品。`；证据 `记录作品中能看出明确变化方向的学生样本。`。
- 选择 `episode_05_self_check_revision`：显示 `自查与微修订`；提示 `评价只抓一处微修，不做大评比。`；证据 `记录修改前后或学习单自查勾选。`。

结果：STATIC_CONTEXT_MAP_READY，preview_only=true，runtime_call_allowed=false。
